apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-shared-storage
  namespace: epd
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client-pokprod
  resources:
    requests:
      storage: 50Gi

# Made with Bob
