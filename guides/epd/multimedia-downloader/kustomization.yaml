apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- service.yaml
- configmap.yaml
- deployment.yaml

labels:
- pairs:
    llm-d.ai/guide: epd
    component: multimedia-downloader
